import React from 'react';

/*---------------------------------------------------------------------------------------------------------*/

function Heading(props) {
  return (
      <>
        {props.h1 ? <>
          <h1 className={props.center ? "text-center" : ""}><b>{props.h1}</b></h1>{props.hr ? <hr /> : <></>}
        </> : <></>}
      </>
  )
}

/*---------------------------------------------------------------------------------------------------------*/

export default Heading;